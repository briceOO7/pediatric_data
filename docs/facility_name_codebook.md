# Facility name codebook (study codes → display names)

Used in **tables and figure labels**. Raw CSVs under `data/` may still use the `code` values for stable joins and scripts.

| Code | Display name |
|------|----------------|
| `CAH_01` | Maniilaq Health Center |
| `Hub_01` | ANMC |
| `OutsideHospital02` | UW |
| `OutsideHospital03` | Providence |

Machine-readable: `docs/facility_name_codebook.csv`.
