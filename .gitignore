# ── Data (PHI / study extracts — never commit) ────────────────────────────────
data/

# ── Generated pipeline outputs (regenerated by running the pipeline) ──────────
outputs/data/
outputs/tables/
outputs/figures/*
!outputs/figures/README.md
outputs/figures_external/
outputs/review/
outputs/review_cc_audit/

# ── Re-identification keys (letter↔real-name maps — never commit) ────────────
# The mapping itself isn't PHI, but it lets anyone reverse the anonymization
# used in manuscripts/paper1/external_manuscript.qmd. Keep local only.
docs/external_village_codebook.csv
docs/external_village_codebook.md

# ── Python ─────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── R / renv ───────────────────────────────────────────────────────────────────
# Track: renv.lock, renv/activate.R, renv/settings.json  (committed — reproducibility)
# Ignore: the actual installed library (large, machine-specific binaries)
renv/library/
renv/local/
renv/cellar/
renv/staging/
vendor/pediatric_medevac_sankey/renv/library/
vendor/pediatric_medevac_sankey/figures/

# ── Quarto / OS ────────────────────────────────────────────────────────────────
.quarto/
_quarto_output/
.DS_Store
*.quarto_ipynb

/.quarto/
**/*.quarto_ipynb
